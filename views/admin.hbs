<div id="content" style="border-top: 1px solid #0060AA;">
    <div class="mainparagraph">
        <h1>{{ title }}</h1>

        <form action="/admin/submit" method="post">
        {{#each data}}
            <div>
                <label class="label" for="headline">Überschrift</label>
                <textarea class="input_field" id="headline" name="headline">{{ headline }}</textarea>
            </div>
            <div>
                <label class="label" for="item">Text</label>
                <textarea class="editor" class="input_field" id="item" name="item">{{{ entry }}}</textarea>
            </div>
        {{/each}}

        <button class="submit_button" type="submit">Speichern</button>
        </form>

    </div>
    <span class="end"></span>
</div>